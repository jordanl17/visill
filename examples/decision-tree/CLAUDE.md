# CLAUDE.md

This directory is a vendored snapshot of `claude-skill-decision-tree`, rewritten to consume the visill framework via `workspace:*`. It serves as the framework's integration gate: any visill SDK / @visill/build / @visill/test regression breaks this example's build or tests before publish.

For framework-level conventions, see the parent repo's CLAUDE.md.
